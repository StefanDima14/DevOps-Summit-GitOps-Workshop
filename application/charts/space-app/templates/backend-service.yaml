apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    {{- include "space-app.labels" . | nindent 4 }}
spec:
  selector:
    app: backend
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: 3000
